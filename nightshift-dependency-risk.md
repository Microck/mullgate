# Nightshift: Dependency Risk Scanner

## Executive Summary

Analyzed the mullgate TypeScript project (2 package.json files, 671 transitive packages). Root CLI has zero vulnerabilities. Docs site has 8 CVEs in transitive dependencies (3 high, 5 moderate), all fixable with version bumps.

**Overall Risk Rating: MEDIUM**

## Findings

### P0 — Critical (Action Required)

1. **`path-to-regexp` ReDoS via `fumadocs-core`** (HIGH)
   - Package: `path-to-regexp@8.3.0` (vulnerable: >=8.0.0 <8.4.0)
   - Path: `fumadocs-core@16.7.4 > path-to-regexp@8.3.0`
   - Fix: Upgrade `fumadocs-core` to >=16.7.9

2. **`picomatch` ReDoS via `eslint-config-next`** (HIGH)
   - Packages: `picomatch@2.3.1` and `picomatch@4.0.3`
   - Path: `eslint-config-next@16.2.1` (multiple transitive chains)
   - Fix: Upgrade `eslint-config-next` to 16.2.2

### P1 — High (Should Fix Soon)

3. **`fumadocs-mdx` version mismatch** — `fumadocs-mdx@14.2.11` vs `fumadocs-core@16.7.4`/`fumadocs-ui@16.7.4`. Major version discrepancy may cause future breakage.

4. **`lucide-react` stuck at ^0.577.0** — caret range can't reach 1.x (latest: 1.7.0). Update to `^1.7.0`.

5. **`brace-expansion` DoS** via eslint chain (MODERATE) — upgrade eslint and eslint-config-next.

6. **`fumadocs-core` and `fumadocs-ui` patch updates** — 16.7.4 to 16.7.9 (includes path-to-regexp fix).

### P2 — Medium (Plan to Address)

7. **Mixed version-pinning strategies** — root prod deps use caret, some devDeps are exact. Standardize.

8. **`@types/node` version mismatch** — root: ^24.10.1, docs: ^25.5.0. Align to same major version.

9. **Outdated pnpm** — 10.18.3 vs latest 10.33.0.

10. **`@biomejs/biome` outdated** — 2.2.6 vs 2.4.10.

### P3 — Low (Monitor)

11. **No lockfile committed** — builds not reproducible.
12. **No pnpm workspace config** — duplicated shared packages at different versions.
13. **`@clack/prompts` bus factor** — niche library, small maintainer team.
14. **Docs site 587 transitive packages** — large supply-chain attack surface.

## Quick Action Checklist

| # | Action | Priority |
|---|--------|----------|
| 1 | Bump `fumadocs-core` -> 16.7.9, `fumadocs-ui` -> 16.7.9 | P0 |
| 2 | Bump `eslint-config-next` -> 16.2.2 | P0 |
| 3 | Update `lucide-react` range -> `^1.7.0` | P1 |
| 4 | Run `corepack use pnpm@10.33.0` | P2 |
| 5 | Commit lockfiles | P3 |

---
*Generated by Nightshift v3 (GLM 5.1) — dependency-risk task*
